oro_dashboard_config:
    widgets:
        oro_bug_chart:
            label: oro.bug.issue.chart
            route: oro_bug_chart_widget
            description: oro.bug.issue.chart
        issues_by_user:
            label: oro.bug.issue.by_user
            route: oro_dashboard_widget        # you can use existing controller to render your TWIG template
            route_parameters: { bundle: OroBugBundle, name: issuesByUser }   # just specify a bundle and a TWIG template name
            acl: bug.issue_view